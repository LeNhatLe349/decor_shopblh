services:
  frontend:
    image: caddy:2-alpine
    ports:
      - "5173:5173"
    command: ["caddy", "run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
    volumes:
      - ./Caddyfile.preview.vibeflow:/etc/caddy/Caddyfile:ro
      - ./Decorshop:/usr/share/caddy:ro